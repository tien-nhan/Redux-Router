import React from "react";
import { useSelector } from "react-redux";
import { Cart, Main } from "../../component";

const Index = () => {
  return (
    <Main>
      <Cart />
    </Main>
  );
};

export default Index;
